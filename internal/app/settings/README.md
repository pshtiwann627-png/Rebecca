# Settings

This package owns persisted panel and subscription settings.

It stores and normalizes panel settings, subscription URL/path/alias/port
settings, admin-specific subscription overrides, and subscription template
content paths.

Certificate management and 3x-ui import are intentionally disabled/TODO and
should be rebuilt as Go-native flows before being re-enabled.
