# Ads

This package is intentionally inactive.

The previous panel ads feature was removed from the runtime. Keep this package
only if transitional code still imports it; otherwise it can be removed once the
remaining references are gone.

New product UI should not depend on ads endpoints or ad-loading behavior.
